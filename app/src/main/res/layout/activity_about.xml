<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AboutActivity">

    <ImageView
        android:id="@+id/aboutLogoImageView"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginBottom="400dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent"
        app:srcCompat="@drawable/logo" />

    <TextView
        android:id="@+id/ownersTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:background="#C6C6C6"
        android:drawableTint="#00FFFFFF"
        android:text="@string/app_owner"
        app:layout_constraintEnd_toEndOf="@+id/aboutLogoImageView"
        app:layout_constraintStart_toStartOf="@+id/aboutLogoImageView"
        app:layout_constraintTop_toBottomOf="@+id/aboutLogoImageView" />

    <TextView
        android:id="@+id/aboutTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:background="#C6C6C6"
        android:text="@string/about_app"
        app:layout_constraintEnd_toEndOf="@+id/aboutLogoImageView"
        app:layout_constraintStart_toStartOf="@+id/aboutLogoImageView"
        app:layout_constraintTop_toBottomOf="@+id/ownersTextView" />
</androidx.constraintlayout.widget.ConstraintLayout>